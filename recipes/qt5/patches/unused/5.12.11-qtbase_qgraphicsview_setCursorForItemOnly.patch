diff --git c/qtbase/src/widgets/graphicsview/qgraphicsitem.cpp i/qtbase/src/widgets/graphicsview/qgraphicsitem.cpp
index dbce80b12..e63c14554 100644
--- c/qtbase/src/widgets/graphicsview/qgraphicsitem.cpp
+++ i/qtbase/src/widgets/graphicsview/qgraphicsitem.cpp
@@ -2344,6 +2344,24 @@ void QGraphicsItem::setCursor(const QCursor &cursor)
     itemChange(ItemCursorHasChanged, cursorVariant);
 }
 
+/*!
+    Sets the current cursor shape for the item to \a cursor, but only the item. 
+    Subitems or items below the cursor are not checked! The mouse cursor
+    will assume this shape when it's over this item. See the \link
+    Qt::CursorShape list of predefined cursor objects\endlink for a range of
+    useful shapes.
+
+    \sa setCursor(), cursor(), hasCursor(), unsetCursor(), QWidget::cursor,
+    QApplication::overrideCursor()
+*/
+void QGraphicsItem::setCursorForItemOnly(const QCursor &cursor)
+{    
+    const QVariant cursorVariant(itemChange(ItemCursorChange, QVariant::fromValue<QCursor>(cursor)));
+    d_ptr->setExtra(QGraphicsItemPrivate::ExtraCursor, qvariant_cast<QCursor>(cursorVariant));
+    d_ptr->hasCursor = 1;
+    itemChange(ItemCursorHasChanged, cursorVariant);
+}
+
 /*!
     Returns \c true if this item has a cursor set; otherwise, false is returned.
 
diff --git c/qtbase/src/widgets/graphicsview/qgraphicsitem.h i/qtbase/src/widgets/graphicsview/qgraphicsitem.h
index c228e765d..68253ed7a 100644
--- c/qtbase/src/widgets/graphicsview/qgraphicsitem.h
+++ i/qtbase/src/widgets/graphicsview/qgraphicsitem.h
@@ -204,6 +204,7 @@ public:
 #ifndef QT_NO_CURSOR
     QCursor cursor() const;
     void setCursor(const QCursor &cursor);
+    void setCursorForItemOnly(const QCursor &cursor);
     bool hasCursor() const;
     void unsetCursor();
 #endif
diff --git c/qtbase/src/widgets/graphicsview/qgraphicsproxywidget.cpp i/qtbase/src/widgets/graphicsview/qgraphicsproxywidget.cpp
index 513cf9d36..8d680ddac 100644
--- c/qtbase/src/widgets/graphicsview/qgraphicsproxywidget.cpp
+++ i/qtbase/src/widgets/graphicsview/qgraphicsproxywidget.cpp
@@ -991,6 +991,16 @@ bool QGraphicsProxyWidget::eventFilter(QObject *object, QEvent *event)
             }
             break;
 #endif
+// Begin MeVis patch:
+#ifndef QT_NO_CURSOR
+        case QEvent::CursorChange:
+            // Propagate cursor change to the proxy.
+            if (d->lastWidgetUnderMouse) {
+                setCursor(d->lastWidgetUnderMouse->cursor());
+            }
+            break;
+#endif
+// End MeVis patch
         default:
             break;
         }
