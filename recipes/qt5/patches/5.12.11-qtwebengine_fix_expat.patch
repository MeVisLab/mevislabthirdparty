diff --git i/qtwebengine/src/3rdparty/chromium/third_party/expat/BUILD.gn w/qtwebengine/src/3rdparty/chromium/third_party/expat/BUILD.gn
index bf6f54b476..7435573cfd 100644
--- i/qtwebengine/src/3rdparty/chromium/third_party/expat/BUILD.gn
+++ w/qtwebengine/src/3rdparty/chromium/third_party/expat/BUILD.gn
@@ -9,7 +9,7 @@ import("//testing/libfuzzer/fuzzer_test.gni")
 # let's not pull it in twice.
 # Chromecast doesn't ship expat as a system library.
 # Libfuzzer and AFL need to build library from sources.
-if (is_linux && !is_chromecast && !use_fuzzing_engine) {
+if (false && is_linux && !is_chromecast && !use_fuzzing_engine) {
   config("expat_config") {
     libs = [ "expat" ]
   }
