diff --git a/CMakeLists.txt b/CMakeLists.txt
index 1242bba..3ca8814 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -598,12 +598,12 @@ ENDIF()
 
 IF( NOT IOS )
   IF( NOT ASSIMP_BUILD_MINIZIP )
-    use_pkgconfig(UNZIP minizip)
+    #use_pkgconfig(UNZIP minizip)
   ENDIF()
 ELSE ()
   IF( NOT BUILD_SHARED_LIBS )
     IF( NOT ASSIMP_BUILD_MINIZIP )
-      USE_PKGCONFIG(UNZIP minizip)
+      #USE_PKGCONFIG(UNZIP minizip)
     ENDIF()
   ENDIF ()
 ENDIF ()
