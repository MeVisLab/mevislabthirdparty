diff --git i/meson.build w/meson.build
index 9fc09a4..6bea743 100644
--- i/meson.build
+++ w/meson.build
@@ -1331,7 +1331,7 @@ else
 endif

 # TODO: some of these may be conditional
-dep_zlib = dependency('zlib', version : '>= 1.2.3', fallback : ['zlib', 'zlib_dep'])
+dep_zlib = dependency('zlib', version : '>= 1.2.3')
 pre_args += '-DHAVE_ZLIB'

 _zstd = get_option('zstd')
@@ -1363,7 +1363,7 @@ if dep_thread.found() and host_machine.system() != 'windows'
   endif
 endif
 if host_machine.system() != 'windows'
-  dep_expat = dependency('expat', fallback : ['expat', 'expat_dep'],
+  dep_expat = dependency('expat',
                          required: not with_platform_android or with_any_broadcom or with_any_intel)
 else
   dep_expat = null_dep
@@ -1512,7 +1512,6 @@ if _llvm != 'disabled'
     ),
     static : not _shared_llvm,
     method : _llvm_method,
-    fallback : ['llvm', 'dep_llvm'],
   )
   with_llvm = dep_llvm.found()
 endif
