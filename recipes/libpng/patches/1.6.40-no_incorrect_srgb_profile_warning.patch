diff --git i/png.c w/png.c
index d6471b0..4719d80 100644
--- i/png.c
+++ w/png.c
@@ -2388,8 +2388,10 @@ png_compare_ICC_profile_with_sRGB(png_const_structrp png_ptr,
                       * discourage their use, skip the 'have_md5' warning below,
                       * which is made irrelevant by this error.
                       */
-                     png_chunk_report(png_ptr, "known incorrect sRGB profile",
-                         PNG_CHUNK_ERROR);
+                     // MeVis: disabled because many PNGs have these incorrect profiles
+                     // and Qt does not make use of them.
+                     //png_chunk_report(png_ptr, "known incorrect sRGB profile",
+                     //   PNG_CHUNK_ERROR);
                   }
 
                   /* Warn that this being done; this isn't even an error since
