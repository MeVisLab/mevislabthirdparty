project(VTKWrapper)

include(${CMAKE_BINARY_DIR}/../conanbuildinfo.cmake)
conan_basic_setup()

set(_UNUSED ${CMAKE_EXPORT_NO_PACKAGE_REGISTRY} ${CMAKE_INSTALL_OLDINCLUDEDIR} ${CMAKE_DEBUG_POSTFIX} ${CONAN_COMPILER_VERSION} ${CMAKE_VS_PLATFORM_TOOLSET_HOST_ARCHITECTURE})

set(LIBXML2_FOUND TRUE)
set(LIBXML2_INCLUDE_DIR ${CONAN_INCLUDE_DIRS_LIBXML2})
set(LIBXML2_LIBRARIES ${CONAN_LIBS_LIBXML2})
set(vtklibxml2_INCLUDE_DIRS ${CONAN_INCLUDE_DIRS_LIBXML2})
set(vtklibxml2_LIBRARIES ${CONAN_LIBS_LIBXML2})
mark_as_advanced(LIBXML2_LIBRARIES LIBXML2_INCLUDE_DIR vtklibxml2_INCLUDE_DIRS vtklibxml2_LIBRARIES)

add_subdirectory(sources)
