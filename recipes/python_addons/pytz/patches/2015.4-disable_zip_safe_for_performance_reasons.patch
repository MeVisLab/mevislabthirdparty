--- a/setup.py
+++ b/setup.py
@@ -25,7 +25,7 @@ assert len(resources) > 10, 'zoneinfo files not found!'
 setup (
     name='pytz',
     version=pytz.VERSION,
-    zip_safe=True,
+    zip_safe=False,
     description='World timezone definitions, modern and historical',
     long_description=open('README.txt','r').read(),
     author=me,
--
2.23.0.windows.1

