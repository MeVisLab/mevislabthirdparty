diff --git i/hdf5-1.12.1/CMakeFilters.cmake w/hdf5-1.12.1/CMakeFilters.cmake
index d5f801e..e0730d6 100644
--- i/hdf5-1.12.1/CMakeFilters.cmake
+++ w/hdf5-1.12.1/CMakeFilters.cmake
@@ -109,7 +109,7 @@ endif ()
 option (HDF5_ENABLE_SZIP_SUPPORT "Use SZip Filter" OFF)
 if (HDF5_ENABLE_SZIP_SUPPORT)
   option (HDF5_ENABLE_SZIP_ENCODING "Use SZip Encoding" OFF)
-  if (NOT SZIP_USE_EXTERNAL)
+  if (FALSE)
     find_package (SZIP NAMES ${SZIP_PACKAGE_NAME}${HDF_PACKAGE_EXT} COMPONENTS static shared)
     if (NOT SZIP_FOUND)
       find_package (SZIP) # Legacy find
@@ -118,7 +118,7 @@ if (HDF5_ENABLE_SZIP_SUPPORT)
       endif ()
     endif ()
   endif ()
-  if (SZIP_FOUND)
+  if (TRUE)
     set (H5_HAVE_FILTER_SZIP 1)
     set (H5_HAVE_SZLIB_H 1)
     set (H5_HAVE_LIBSZ 1)
@@ -145,8 +145,8 @@ if (HDF5_ENABLE_SZIP_SUPPORT)
       message (FATAL_ERROR "SZIP is Required for SZIP support in HDF5")
     endif ()
   endif ()
-  set (LINK_COMP_LIBS ${LINK_COMP_LIBS} ${SZIP_STATIC_LIBRARY})
-  INCLUDE_DIRECTORIES (${SZIP_INCLUDE_DIRS})
+  set (LINK_COMP_LIBS ${LINK_COMP_LIBS} ${CONAN_LIBS_LIBAEC})
+  INCLUDE_DIRECTORIES (${SZIP_INCLUDE_DIRS} ${CONAN_INCLUDE_DIRS_LIBAEC})
   if (CMAKE_VERSION VERSION_GREATER_EQUAL "3.15.0")
     message (VERBOSE "Filter SZIP is ON")
   endif ()
