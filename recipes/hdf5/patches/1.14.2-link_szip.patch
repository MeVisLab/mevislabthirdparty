diff --git i/hdf5-1.14.2/CMakeFilters.cmake w/hdf5-1.14.2/CMakeFilters.cmake
index 6573ee3..bbbf8e0 100644
--- i/hdf5-1.14.2/CMakeFilters.cmake
+++ w/hdf5-1.14.2/CMakeFilters.cmake
@@ -147,7 +147,7 @@ if (HDF5_ENABLE_SZIP_SUPPORT)
         find_package (SZIP) # Legacy find
       endif ()
     endif ()
-    if (SZIP_FOUND)
+    if (TRUE)
       set (SZIP_INCLUDE_DIR_GEN ${SZIP_INCLUDE_DIR})
       set (SZIP_INCLUDE_DIRS ${SZIP_INCLUDE_DIRS} ${SZIP_INCLUDE_DIR})
       set (LINK_COMP_LIBS ${LINK_COMP_LIBS} ${SZIP_LIBRARIES})
@@ -163,14 +163,14 @@ if (HDF5_ENABLE_SZIP_SUPPORT)
       EXTERNAL_SZIP_LIBRARY (${HDF5_ALLOW_EXTERNAL_SUPPORT} ${HDF5_ENABLE_SZIP_ENCODING})
       message (VERBOSE "Filter SZIP is built")
       message (VERBOSE "... with library AEC")
-      set (LINK_COMP_LIBS ${LINK_COMP_LIBS} ${SZIP_STATIC_LIBRARY})
     endif ()
   endif ()
-  if (SZIP_FOUND)
+  if (TRUE)
     set (H5_HAVE_FILTER_SZIP 1)
     set (H5_HAVE_SZLIB_H 1)
     set (H5_HAVE_LIBSZ 1)
-    INCLUDE_DIRECTORIES (${SZIP_INCLUDE_DIRS})
+    set (LINK_COMP_LIBS ${LINK_COMP_LIBS} ${CONAN_LIBS_LIBAEC})
+    INCLUDE_DIRECTORIES (${SZIP_INCLUDE_DIRS} ${CONAN_INCLUDE_DIRS_LIBAEC})
     message (VERBOSE "Filter SZIP is ON")
     if (H5_HAVE_FILTER_SZIP)
       set (EXTERNAL_FILTERS "${EXTERNAL_FILTERS} DECODE")
